**What does this change**


**Why**


**Testing**
- [ ] Added or updated a test under `tests/`
- [ ] `pytest -q` passes locally
- [ ] No real API key, token, or PII anywhere in the diff (including test fixtures)
- [ ] If this touches PII masking or local mode: confirmed the privacy contract still holds (nothing leaves the caller's machine unless explicitly configured to)
